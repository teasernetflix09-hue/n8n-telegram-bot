services:
  - type: web
    name: n8n-telegram-ai-bot
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: GOOGLE_API_KEY
        sync: false
      - key: REPLICATE_API_TOKEN
        sync: false
